// Package pipe provides standardized configuration structures for all
// pipe components. Each component defines its config struct with yaml tags
// that map directly to Vault secret keys.
package pipe
